# 截图工具四项优化实现计划

## 功能清单

| 序号 | 功能 | 状态 |
|---|---|---|
| 1 | 自动后缀（提示+自动） | 待实现 |
| 2 | 通知栏（悬浮窗底部状态提示） | 待实现 |
| 3 | 记住上次保存路径（config.json） | 待实现 |
| 4 | `~` 键全局截图（RegisterHotKey） | 待实现 |

## 实现步骤

### 步骤1：读取现有代码，理解结构
- 重读 screenshot_tool.py 确认所有细节

### 步骤2：实现配置持久化（记住上次路径）
- 新增 `_load_config()` 方法：读取 config.json
- 新增 `_save_config()` 方法：写入 config.json
- 修改 `__init__`：启动时尝试加载配置，路径有效则跳过选择
- 修改"更改路径"按钮回调：保存后更新 config.json

### 步骤3：实现自动后缀
- 修改 `_capture()` 中的文件存在处理逻辑
- 新增 `_find_available_filename()` 方法：扫描目录找到最小可用编号
- 将原来的覆盖确认弹窗改为"自动保存为 xxx_N.png"提示弹窗

### 步骤4：实现通知栏
- 在 `_build_ui()` 底部新增一个 Label 作为通知栏
- 新增 `_set_status()` 方法：更新通知栏文字和颜色
- 初始显示"📌 就绪"，截图成功显示"✅ 已保存: xxx.png"
- 修改窗口高度从 215 → ~260（增加通知栏+快捷键提示空间）

### 步骤5：实现 `~` 键全局截图
- 使用 ctypes 调用 user32.dll 的 RegisterHotKey
- 注册 VK_OEM_3（`~` 键）为全局热键
- 在 Tkinter 中处理 WM_HOTKEY 消息
- 界面上添加"快捷键: ~"提示文字
- 新增 `_handle_hotkey()` 方法：收到热键消息时调用截图
- 新增 `_cleanup()` 方法：退出时注销热键

### 步骤6：更新 requirements.txt（无需变更）
- RegisterHotKey 使用 ctypes，零额外依赖

### 步骤7：验证
- 测试所有功能是否正常工作
